/** Entry for the app-header component: types + class. */
export { AppHeader, APP_HEADER_COMMAND } from "./_AppHeader";
export {
  APP_TITLE_ATTRIBUTE,
  APP_VERSION_ATTRIBUTE,
  HEADER_ATTRIBUTE,
  SHELL_PROPS_META_NAME,
  orderedActions,
  readShellProps,
  resolveAppTitle,
  resolveAppVersion,
} from "./types";
export type {
  AppHeaderConfig,
  ShellAction,
  ShellCommand,
  ShellProjectProvider,
  ShellProps,
} from "./types";
